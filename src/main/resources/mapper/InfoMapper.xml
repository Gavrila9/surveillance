<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhis.surveillance.dao.InfoMapper">
    <select id="queryList" resultType="com.dhis.surveillance.vo.DataVO">
        select s.name, s.province, s.city, s.longitude, s.latitude, s.status, s.confirmed_date, s.cure_date, s.death_date from surveillance_system_data s
    </select>
</mapper>